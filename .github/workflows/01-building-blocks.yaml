name: 01 - Building Blocks

on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, world!"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Normal step
        run: |
          echo "I will succeed"
      - name: Say goodbye
        run: echo "Goodbye!"
